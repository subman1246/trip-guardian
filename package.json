{
  "name": "trip-guardian",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomous travel agent that defends a live itinerary and a fixed budget in real time.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "world": "tsx scripts/show-world.ts",
    "tools-demo": "tsx scripts/tools-demo.ts",
    "agent-demo": "tsx scripts/agent-demo.ts",
    "verify-scenario": "tsx scripts/verify-scenario.ts",
    "demo": "tsx scripts/serve.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@google/genai": "^2.12.0"
  }
}
